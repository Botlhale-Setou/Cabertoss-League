const readline = require('readline');
const fs = require('fs');

const capture = (pdb) => {
	if ()
	recordThrows();
};

const recordThrows = async () => {
	// Render header
	console.log('\n\n========================\n   CAPTURE THROWS\n========================\n')

	const reader = readline.createInterface({input: process.stdin, output: process.stdout});
			
	const score = reader.question(`Enter ${pDBSorted[x].name}'s score (Throw #${i + 1}): `);
	pDBSorted[x].scores[i] = score;
	console.log(pDBSorted[x]);
	reader.close();
};

module.exports = capture;